local fidget = require("fidget")

fidget.setup({
  notification = {
    window = {
      winblend = 0,
      border = 'rounded',
    },
    -- override_vim_notify = true,
    -- filter = vim.log.levels.TRACE,
  },
})
